SELECT E.NAME FROM EMP E WHERE EXISTS(SELECT D.DNO FROM DEPT D WHERE E.DEPTTNO=D.DNO AND E.DEPTNO=5):



SELECT D.DNAME,COUNT(*) FROM EMP E,DEPT D WHERE E.DEPTNO=D.NO GROUP BY D.DNAME HAVING COUNT(*)>=2;
